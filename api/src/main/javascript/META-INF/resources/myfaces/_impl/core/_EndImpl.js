/*last file loaded, must restore the state of affairs*/
if(myfaces._impl.core._Runtime._oldExtends) {
    window._MF_CLS = myfaces._impl.core._Runtime._oldExtends;
}
if(myfaces._impl.core._Runtime._oldSingleton) {
   window._MF_SINGLTN = myfaces._impl.core._Runtime._oldSingleton;
}


